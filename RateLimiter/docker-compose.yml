services:
  api1:
    build: .
    ports:
      - "5001:8080"
    environment:
      - Redis__ConnectionString=redis:6379
    depends_on:
      - redis

  api2:
    build: .
    ports:
      - "5002:8080"
    environment:
      - Redis__ConnectionString=redis:6379
    depends_on:
      - redis

  api3:
    build: .
    ports:
      - "5003:8080"
    environment:
      - Redis__ConnectionString=redis:6379
    depends_on:
      - redis

  redis:
    image: redis:8